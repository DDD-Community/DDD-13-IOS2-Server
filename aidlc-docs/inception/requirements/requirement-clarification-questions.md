# Round 2 — 재확인 (FC-8~13)

> 1라운드 답변 반영 후 남은 결정만. `[Answer]:` 에 답.

## 확정된 사항 (참고 — 다시 안 물음)
- 범위: AI가 적절한 context 단위로 분할 / 푸시알림 전부 제외(Q1·Q2)
- LocationStatus → BEFORE/RECOMMENDED/VOTING/CONFIRMED 4-state 교체(Q3)
- 자동전환 시 투표 마감 기본 +3일(Q5)
- 이동부담: subway_edge 메모리 그래프 + 다익스트라, 결과 DB 스냅샷(Q6)
- 환승 = 최단경로상 TRANSFER 엣지 수(Q6-1)
- 추천 0개면 반경 확대 재시도(Q7-1)
- 투표 다중제한 PRD 그대로 / 익명(member_id 저장, 집계만 노출)(Q11·Q12)
- 확장: Security ON / PBT Partial / TDD OFF(Q13~15)

---

## R1. FC-8 추천 모델 — 본인 모델로 확정 (Q8·Q9 통합)
처리:
1. 참여자 중심 기준 중간지점 역 3개 (기존 구현 재사용)
2. 3개 역 반경 N km 내 장소 수집 + 하드필터(예약/주차 등 요청 옵션)
3. 점수 = `w1·(참여자들→장소 총/평균 이동거리 역수) + w2·(옵션·분위기 태그 매칭) [+ w3·rating]`
4. 점수순 상위 15개
5. 각 장소를 3개 역 중 최근접 역에 라벨링(=역 귀속) → FC-9 역 탭 필터용

### R1-1. 이 모델로 진행?
- A) 그대로 OK ✅
- X) 수정(아래 기술)

[Answer]: X 이대로 좋기는한데, 나는 예시를 든거고 참여자들 ->장소 총/평균 이동시간이 구하려면 좀 오래걸리지않나? 뭐든 적절한걸로 했으면 좋겠어 가중치도 적당한 정규화가 필요할텐데 이건 어떻게 할지 모르겠는데 너가 해줄꺼야?

### R1-2. FC-9 "역 탭"(역 선택 시 해당 역 장소만 노출) 기능 유지?
- A) 유지 (→ 역 귀속 라벨 필요)
- B) 제거 (역 구분 없이 15개 한 리스트) → 역 귀속 불필요
- X) 기타

[Answer]: A (역탭 필요하다니까 각 장소 후보에 포함되는 역을 구해야할것 같네...?)

### R1-3. 점수 가중치(w1·w2·w3) 결정 시점
- A) 설계(Application Design) 단계에서 구체 수치 확정 ✅추천
- B) 지금 바로 값 지정(아래 기술)
- X) 기타

[Answer]: A

---

## R2. 장소 탐색 반경 N km 파라미터화
현재 station 탐색은 2km 하드코딩. 장소 탐색 반경을 조절 가능하게.

### R2-1. 반경 값을 어떻게 받나?
- A) 장소선정 시작 API 요청 파라미터로 (호스트가 지정, 기본 2km) ✅추천
- B) 모임 생성 시 설정값으로 고정
- C) 전역 상수(설정파일)로만, 요청별 변경 X
- X) 기타

[Answer]: X 나는 기본 2키로로 하고 역이 없으면 4키로 6키로 이런식으로 늘어나면 되지 않을까 생각한건데? 대신 최대 반경은... 6km로 하는게 좋을것같고 없으면 어떡하지..? ㅋㅋ

### R2-2. 기본값
- A) 2km ✅
- X) 기타(값 기술)

[Answer]: A

---

## R3. 담기 마감 시 후보 0개 처리 (Q4 답변 구체화)
답변: "마감일 지나면 15개 중 스코어 상위 3개를 후보로 올려 투표 진행".

### R3-1. 적용 조건
- A) 아무도 안 담았을 때(0개)만 top-3 자동 등록 후 VOTING ✅추천
- B) 담긴 후보가 3개 미만이면 항상 top-스코어로 3개 채움
- X) 기타

[Answer]: A

### R3-2. 자동 등록 개수
- A) 3개 고정 ✅(답변대로)
- X) 기타(값)

[Answer]: A

---

## R4. 역별 편중 (Q7 재질문)
거리 점수로 뽑으면 15개가 한 역에 몰릴 수 있음.

- A) 점수순 그대로, 역별 최소보장 없음 ✅추천(본인 모델과 일치)
- B) 역별 최소 N개 보장
- X) 기타

[Answer]: A

---

## R5. 이동부담 스냅샷 저장 (Q6 확인)
계산 결과(참여자×후보: 소요초·환승수)를 신규 DB 테이블에 저장. subway_edge는 부팅 시 메모리 로드.

- A) OK (DB 테이블 저장) ✅
- X) 기타

[Answer]:  X 이걸 DB 에저장하는게 맞나 ..? 매번 구하게하던가 아니면 캐싱 하게 하던가 하는게 더 맞지 않나 싶은데 ..? 꼭 DB에 저장해야해?

---

## R6. FC-9 카드 "거리" 값 (Q10 확인)
추천 응답의 각 장소에 "유저 출발지 기준 거리" 포함. 어떤 거리?

- A) 직선거리(PostGIS ST_Distance) ✅추천(가벼움, 추천 점수의 이동거리와 동일 소스)
- B) subway_edge 그래프 기반 거리
- X) 기타

[Answer]: B(해당 장소에서 가장 가까운 역- 보고있는 사람의 출발지 이동거리를 구한 값)
